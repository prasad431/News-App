name: news_app
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^7.0.0
  flutter_bloc: ^7.0.1
  equatable: ^2.0.3
  webview_flutter: ^2.0.10

  
dev_dependencies:
  flutter_test:
    sdk: flutter
  intl: ^0.17.0
  http: ^0.13.3
  bloc_test: ^8.0.2
  mockito: ^5.0.11
  build_runner: ^2.0.6

flutter:
  uses-material-design: true
